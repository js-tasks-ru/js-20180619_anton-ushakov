/**
 * Power
 * @param {number} m base
 * @param {number} n index
 * @returns {number}
 */
function pow (m, n) {
    while ( n = 0){
        var result =(m * m);
        n = n - 1;
    }
    
        return result;
}

//console.log(pow(2,3));